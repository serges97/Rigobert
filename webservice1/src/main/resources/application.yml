spring:
  application:
    name: Webservice1
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
  healthcheck:
    senabled: true
server:
  port: 8181